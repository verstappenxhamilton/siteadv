services:
  - type: web
    name: triage-chat
    env: node
    plan: free
    nodeVersion: 20
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
